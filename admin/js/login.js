// ── Auth removed — redirect straight to the dashboard ─────────────────
// The login form is no longer functional. Users land here and are sent
// directly to dashboard.html. If you re-add auth in the future, restore
// the form submission logic below.
window.location.replace('dashboard.html');
